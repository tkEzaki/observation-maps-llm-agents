"""Main-text figure set — redesigned variant.

Renders at true print size (180 mm double column) with journal typography.
Outputs to the earlier figure layout; reads the same data as analysis.figure_data.
"""
